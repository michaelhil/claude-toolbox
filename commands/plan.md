---
description: Draft a spec, plan, and task breakdown for a feature or change using the sdd skill. Pairs with stress-test for review.
---

Invoke the `sdd` skill to draft spec → plan → tasks for the following request:

$ARGUMENTS

If `$ARGUMENTS` is empty, ask the user what they want to plan before invoking the skill.

After the skill returns the three artifacts, remind the user: "Run `stress-test` on this plan before implementing."
